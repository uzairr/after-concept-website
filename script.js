const fs = require('fs'); const code = fs.readFileSync('app/page.tsx', 'utf8'); const start = code.indexOf('{/* Hero Section */}'); const end = code.indexOf('<section className="origin">'); console.log(start, end); if (start > -1 && end > -1) { const replaceStr = code.substring(start, end); console.log('Replacing length', replaceStr.length); fs.writeFileSync('app/page.tsx', code.substring(0, start) + '<HeroComponent />\n\n      ' + code.substring(end)); }
